# Day 3: Colors

## Topics
- `fill()` and `stroke()` for coloring shapes
- `noFill()` and `noStroke()` for removing fill/outline
- `strokeWeight()` for line thickness
- Alpha transparency values
- `background()` for canvas color

## Activity: Self-Portrait
- **Paper sketch**: Plan a portrait using shapes learned so far
- **Peer consultation**: Share sketches, get shape suggestions
- **Begin coding**: Start with a basic face outline
- Will finish and do a gallery walk next class

## Homework
- Review anything from last two classes as needed
